# =============================================================================
# API Key Manager - Production Environment Configuration
# =============================================================================

# -----------------------------------------------------------------------------
# Database Configuration (PostgreSQL)
# -----------------------------------------------------------------------------
# Production database URL - typically provided by hosting platform
DATABASE_URL="postgresql://username:password@host:5432/database_name"

# -----------------------------------------------------------------------------
# Authentication Configuration (SECURE VALUES REQUIRED)
# -----------------------------------------------------------------------------
# Strong JWT secret - generate with: openssl rand -base64 32
JWT_SECRET="your-secure-jwt-secret-32-chars-minimum"

# Strong admin password
ADMIN_PASSWORD="your-secure-admin-password"

# -----------------------------------------------------------------------------
# API Configuration (SECURE VALUES REQUIRED)
# -----------------------------------------------------------------------------
# Secure service API key - generate with: openssl rand -base64 32
SERVICE_API_KEY="your-secure-service-api-key"

# -----------------------------------------------------------------------------
# Application Configuration
# -----------------------------------------------------------------------------
NODE_ENV="production"

# =============================================================================
# Deployment Platform Examples:
# =============================================================================

# Heroku:
# - DATABASE_URL is automatically provided by Heroku Postgres addon
# - Set other variables via: heroku config:set VAR_NAME=value

# Railway:
# - DATABASE_URL is automatically provided by Railway Postgres
# - Set variables in Railway dashboard

# Vercel:
# - Add PostgreSQL database (Neon, PlanetScale, etc.)
# - Set variables in Vercel dashboard

# Docker:
# - Use docker-compose.yml or environment files
# - Mount this file as environment configuration
# =============================================================================