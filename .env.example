# =============================================================================
# API Key Manager - Environment Configuration
# =============================================================================

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------
# The system automatically detects the database provider based on DATABASE_URL:
# - SQLite: file:./path/to/database.db
# - PostgreSQL: postgresql://user:password@host:port/database

# Development (SQLite) - Default for local development
DATABASE_URL="file:./dev.db"

# Production (PostgreSQL) - Uncomment and configure for production:
# DATABASE_URL="postgresql://username:password@localhost:5432/api_key_manager"

# Heroku PostgreSQL example:
# DATABASE_URL="postgres://user:password@host:5432/database"

# -----------------------------------------------------------------------------
# Authentication Configuration
# -----------------------------------------------------------------------------
# JWT secret for session management - MUST be changed in production
JWT_SECRET="your-jwt-secret-key-change-this-in-production"

# Admin password for accessing the management interface
ADMIN_PASSWORD="your-admin-password"

# -----------------------------------------------------------------------------
# API Configuration
# -----------------------------------------------------------------------------
# Service API key for protecting the validation endpoint
# External services must provide this key to validate API keys
SERVICE_API_KEY="your-service-api-key"

# -----------------------------------------------------------------------------
# Application Configuration
# -----------------------------------------------------------------------------
# Environment mode - automatically detected but can be overridden
NODE_ENV="development"

# =============================================================================
# Production Security Notes:
# - Use strong, unique values for all secrets
# - JWT_SECRET should be at least 32 characters
# - ADMIN_PASSWORD should be complex and unique
# - SERVICE_API_KEY should be a long, random string
# - Never commit actual secrets to version control
# =============================================================================